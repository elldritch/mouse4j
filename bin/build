#!/usr/bin/env bash

cd "$(dirname "$0")/.."

# Ensure cson is installed.
npm install cson

# Generate package.json.
node ./node_modules/cson/bin/cson2json package.cson > package.json

# Install dependencies.
npm install
